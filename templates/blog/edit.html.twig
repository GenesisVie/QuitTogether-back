{% extends 'base.html.twig' %}

{% block title %}Modification du Blog{% endblock %}


{% block body %}
    <style>
        body {

            background-repeat: no-repeat;
            background-color: #003f5c;
            margin-top: 50px;
            color: #fb5b5a;
            text-align: center;
        }
        .table td
        {
            border:none;
            color: #fb5b5a;
        }
        .table th
        {
            border:none;
            color: #fb5b5a;
        }
        h1
        {
            color: #fb5b5a;
            font-size: 80px;
        }
        .btn
        {
            margin-top: 20px;
        }
    </style>
    <h1>Modifier un article</h1>

    {{ include('blog/_form.html.twig', {'button_label': 'Modifier'}) }}
<button type="button" class="btn btn-dark">
    <a href="{{ path('blog_index') }}">Retourner a la liste</a>
</button>

    {{ include('blog/_delete_form.html.twig') }}
{% endblock %}
